# ブラウザ検証記録

実行環境：Chromium 144.0.7559.96 / Python 3.13 / Playwright。2026年9月5日に実行。

**48項目を検証し、すべてPASS。一連の操作中の未処理JavaScript例外は0件でした。**

## 確認方法と制約

実際のChromiumに単体HTMLの内容を読み込ませ、DOM操作・マウスのドラッグ・Canvas描画・ファイル選択・ダウンロードまで実行しました。出力したPNG/JPEGを別の画像ライブラリで開き、形式とピクセルサイズも確認しています。正常なプロジェクトの再読込と、壊れたプロジェクトを拒否した後の既存状態の保持を確認しました。

検証用ブラウザではローカルURLへの通常のナビゲーションが環境ポリシーで拒否されたため、HTMLをDOMに直接読み込む方法を使用しています。したがって、ダブルクリックしたfile://起動やHTTP配信そのものを、この環境で実機確認したという意味ではありません。分割版はclassic scriptと相対パスで構成し、直接起動できるように実装しています。

外部通信を遮断した状態でテストしました。Google Fontsの実際の900ファイルの取得成功、および指定の素材サイトとの直接接続は未確認です。フォント失敗時の警告・書き出しブロック・明示許可後の出力は確認しています。出力テストには環境内の代替フォントを使用し、それらのフォントファイルは配布物へ含めていません。

このDOM実行環境はオリジンの保存領域を許可しません。そのためIndexedDBの実永続化・再起動復元はここでは実機確認できておらず、利用できない際のエラー表示を確認しています。編集ファイルへの保存と再読込は実行済みです。Clipboardへの画像コピー、実機のタッチ操作、Windows版Chrome/Edge、Safari/Firefoxは未確認です。

## 実行した項目

| # | 項目 | 結果 |
|---|---|---|
| 1 | 起動・ロゴ画像の復元 | PASS |
| 2 | JavaScript例外なし（起動） | PASS |
| 3 | 通信遮断時のフォント警告 | PASS |
| 4 | 保存領域制限時の代替案表示 | PASS |
| 5 | 赤文字の実寸フィット・縁取りの安全余白 | PASS |
| 6 | 白文字は赤文字より小さい | PASS |
| 7 | 長い赤文字ほど縮小 | PASS |
| 8 | 長い白文字を2行へ自動分割 | PASS |
| 9 | 背景ファイルをUIから追加 | PASS |
| 10 | 選択部分だけ黄色に変更 | PASS |
| 11 | 先頭に文字を挿入しても強調範囲を追従 | PASS |
| 12 | 部分的な白戻し | PASS |
| 13 | ロゴ左上・黒帯と自由文字は反対側へ | PASS |
| 14 | 透過キャラクターの余白を自動除去 | PASS |
| 15 | キャラクターを背景と文字の間に配置 | PASS |
| 16 | 追加画像5枚で追加ボタン無効 | PASS |
| 17 | API側でも6枚目を拒否 | PASS |
| 18 | 座標・回転・不透明度を数値で編集 | PASS |
| 19 | キャンバスで画像をドラッグ | PASS |
| 20 | 四隅ハンドルによる等比拡大 | PASS |
| 21 | 回転ハンドル・Shiftで15度刻み | PASS |
| 22 | Shift＋矢印で10px移動 | PASS |
| 23 | ロックした位置は移動しない | PASS |
| 24 | レイヤー非表示 | PASS |
| 25 | 画像の削除 | PASS |
| 26 | 削除を取り消す | PASS |
| 27 | 削除をやり直す | PASS |
| 28 | 複製でも上限5枚を保つ | PASS |
| 29 | 日本語変換中は組版・undoを実行しない | PASS |
| 30 | 日本語変換確定を反映 | PASS |
| 31 | 入力文字をHTMLとして実行しない | PASS |
| 32 | 自由文字は指定3色のみ | PASS |
| 33 | 素材HTMLを安全に解析・高解像度リンクを優先 | PASS |
| 34 | 指定サイト取得失敗を明示 | PASS |
| 35 | 指定フォント未読込なら書き出しをブロック | PASS |
| 36 | PNGを1920×1080で実際に書き出し | PASS |
| 37 | JPEGを2560×1440で実際に書き出し | PASS |
| 38 | ガイド表示の有無で完成PNGは変わらない | PASS |
| 39 | 画像を内包した編集プロジェクトを保存 | PASS |
| 40 | 保存データにフォントバイナリなし | PASS |
| 41 | プロジェクト再読込で文章・強調・重ね順・変形を復元 | PASS |
| 42 | 再読込時の画像ID衝突を回避 | PASS |
| 43 | 壊れたプロジェクトは既存状態・素材を変えずに拒否 | PASS |
| 44 | 読み込みプロジェクトにも上限検証 | PASS |
| 45 | 拡張子を偽装したSVGを拒否 | PASS |
| 46 | 390px幅で横スクロールなし | PASS |
| 47 | モバイルでも書き出しダイアログが収まる | PASS |
| 48 | 一連のUI操作中にJavaScript例外なし | PASS |

## 再実行

ツールの使用にはPythonは不要です。以下は開発時のテスト用です。

```sh
python -m pip install -r tests/requirements.txt
python -m playwright install chromium
python build_standalone.py
python tests/browser_regression.py
```

既存のChromiumを使う場合は環境変数 `CHROMIUM_EXECUTABLE` に実行ファイルのパスを指定できます。テストはネットワークに依存しない図形画像を自動生成します。結果・画像は `tests/output/` に保存します。

詳細な計測値は `tests/test-report.json` に記録しています。
