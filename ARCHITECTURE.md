# 実装メモ

## ファイルの役割

| ファイル | 役割 |
|---|---|
| index.html | 3つの編集タブ、プレビュー、素材・出力・保存ダイアログ |
| styles.css | G-Force UIのトークンを使ったエディタ固有のレイアウト、レスポンシブ対応 |
| vendor/gforce.css / gforce.js | 指定UIライブラリのビルド済み実装 |
| assets.js | 提供されたCitrusロゴのPNGをdata URLとして格納 |
| core.js | 文書初期値・検証、文字計測、組版、描画、ヒットテスト、配置候補の評価 |
| sources.js | 外部素材一覧・JSON/HTML読込、URL検証、CORS対応の画像取得 |
| catalog.js / update_catalog.py | 指定サイトの素材URLと名前を同梱。更新時にJSONとして抽出し、サイトのJavaScriptは実行しない |
| app.js | 入力、履歴、ドラッグ、フォント確認、画像正規化、保存、書き出し |
| build_standalone.py | 外部フォント以外を単体HTMLへ結合。Python標準ライブラリのみ |

## 文書と画像

`state` はJSON化できるプレーンな値だけを持ちます。画像本体を文書・履歴へ毎回複製せず、asset IDで参照します。画像Mapには画像本体と正規化したdata URL、小さいアルファチャンネルのヒットテスト用キャッシュを持ちます。

Undo/Redoは文書のスナップショットです。連続入力は650ms内で同一操作としてまとめ、ドラッグはpointerdown〜pointerupを1操作とします。現在の文書・履歴・Redo・最近の24素材から参照されないメモリ上の画像は整理します。ブラウザ永続領域には現在の文書と最近の24素材を保持します。

保存成功の表示にはリビジョン番号を使い、古い非同期保存が完了しても新しい未保存の変更を「保存済み」にしないようにしています。JSONプロジェクトは全画像の検証・デコードが終わってから一括で文書へ反映します。

## 座標と変形

すべて1280×720の論理座標で処理します。画像・自由文字のx/yは中心、角度は度、画像wは幅、不透明度は0〜1です。ハンドルによる拡縮は対角の固定点を基準に等比変形し、回転済みのローカル軸へポインタを逆変換します。

ドラッグ中のキャンバスは1倍解像度、停止時は2倍です。ガイドと選択枠は独立したCanvasにのみ描画します。画像の透過領域は小さいアルファマスクで判定し、透過部分から奥のオブジェクトを選べるようにしています。

## 組版

`measureText()` のadvance幅に加え、actualBoundingBoxの左右・上下を使います。字形サイズに比例した縁取り・影の余裕も計測に加えます。18回の二分探索で最大フォントサイズを求めます。

赤文字は横幅1252px以内、縦244px以内。下端18pxの余白を確保します。白文字は最大2行、幅1228px以内で、赤文字の10px上へ置きます。白文字の部分強調はtextareaのUTF-16選択範囲と同じインデックスを保持します。Intl.Segmenterがある環境では書記素単位で描画します。

縁取りを先に全字描き、その後に塗りを全字描きます。隣の字の縁取りが、先に塗った字を覆うことを防ぎます。赤文字のみNoto Serif JP 900、それ以外はNoto Sans JP 900です。

黒帯は高さ126px、文字は最大68px、幅は文字幅＋64pxです。参考画像の採色値からグラデーションを固定し、論理座標のy=32〜100で #FFFE00 → #FFB800 にしています。塗りだけを描き、文字の縁取り・影は付けません。

赤文字の `headlineTracking` は−100〜100（初期0）、`headlineAlign` はleft/center（初期center）。左寄せは縁取りを含む左端14px、中央寄せは中心640px。旧プロジェクトで配置が未設定ならcenterを補い、文字間は新しい範囲へ制限する。他の文字の文字間仕様は維持する。

## フォント確認

Google FontsのCSSは初期UIをブロックしない状態で読み込み、その後有効化します。`document.fonts.load()` の返却が空の場合を成功扱いにしません。指定ファミリーに対応する実際のFontFaceがloadedとなったことを確認します。

読み込み後は計測キャッシュを破棄し、プレビュー・書き出しプレビューを再組版します。失敗時は代替フォント使用を明示し、明示許可なしの書き出しを止めます。ユーザーによるローカルフォント指定では、ファイルの種類の確認はユーザーに委ねています。

## 画像・外部データ

ラスター画像だけを受け付け、拡張子ではなく先頭バイトも調べます。読込後はCanvasで再エンコードします。SVG/HTMLを画像素材として実行しません。URLはHTTP(S)だけに制限します。外部画像はCORS付きfetch→Blob→data URLとして取り込み、CORSなしの画像を直接合成キャンバスへ描きません。

HTML素材一覧はDOMParserで別ドキュメントとして解析し、アプリへHTMLとして挿入しません。ユーザー文字列はtextContent/valueで扱います。URL・マニフェストは最大件数と長さを制限します。外部プロキシ、認証情報の送信、サーバー側スクレイピングはありません。

## 公開インターフェース

`window.TFTCore` にレイアウト・描画・検証関数、`window.TFTSources` に素材パーサー、`window.TFTApp` にテスト用の読み取りと入出力関数を公開しています。`getState()` / `getScene()` はコピーを返し、可変の内部参照を公開しません。

## 意図的に含めないもの

AIによる背景除去、顔認識、サーバー送信、YouTube投稿、ゲーム画像本体、フォントバイナリ、クラウド同期は含めていません。
