# TFT サムネイル編集

## 開き方

フォルダを展開して **index.html を Chrome または Edge で開いてください**。ビルド、npm、サーバーの起動、アカウント登録は不要です。ファイルは同じフォルダ構成のまま置いてください。

別配布の **TFT-Thumbnail-Editor.html** は、CSS・JavaScript・Citrusロゴをまとめた単体版です。機能は分割版と同じです。

Google Fonts から Noto Serif JP 900 / Noto Sans JP 900 を読み込むため、指定の字形を使う初回起動にはインターネット接続が必要です。フォントファイルは同梱していません。

## 最短の操作

1. 「背景を選ぶ」で画像を選びます。キャンバスへのドロップ、Ctrl+Vで画像を貼り付ける方法もあります。
2. 「メインの赤文字」「上の白文字」を入力します。サイズと改行は自動です。
3. 「書き出す」からPNGをダウンロードします。標準は1280×720です。

入力するたびにプレビューが更新されます。「生成」を押す必要はありません。ロゴは初期状態で右上に配置されます。

## 文字

### メインの赤文字

Noto Serif JP、ウェイト900です。赤塗り、内側の白縁、外側の黒縁をCanvasで重ねて描画します。文字の実際の描画範囲と縁取りを測って、1行に収まる最大サイズを求めます。横に潰す処理は行いません。短い文章が画面全体を埋めないよう、縦方向にも上限を設けています。

文字間は−100%〜100%、新規作成時は0%です。マイナスで詰め、プラスで広げます。従来と同じ調整単位（100%で文字サイズの8%分）を使います。「左寄せ」「中央寄せ」を選択でき、初期設定は中央寄せです。文字間と配置は自動保存・プロジェクト保存・Undo/Redoの対象です。保存済みの文字間は新しい範囲内に収めて復元します。

### 上の白文字

Noto Sans JP、ウェイト900、黒縁です。赤文字の46%を出発点とし、最大88px（1280×720の基準座標）に制限します。1行では基準の78%より小さくなる文章は、自動で2行に分け、幅と高さを再計算します。行頭の句読点、閉じ括弧、英数字の単語途中などを避ける簡易禁則処理があります。明示的な改行も2行まで使えます。「1行」指定で自動改行を止められます。

入力欄で文字を選択して「選択部分を黄色」を押してください。「白に戻す」も部分選択で使えます。後から前後に文字を挿入した場合も、既存の強調範囲をできるだけ引き継ぎます。

### 自由文字

初期のSET18は、ロゴと反対側に自動配置します。黒帯を表示すると、その下へ移動します。初期サイズは赤文字との比率と指定幅から決定します。長い文章は小さくなるため、短い見出し向けです。

「追加」で自由文字を増やせます。プレビュー上で移動・等比拡縮・回転ができ、「重ね順」では3行までの文章、色、大きさ、不透明度、白い外縁も編集できます。色は赤・白・黄の3色だけです。自由文字は最大20個です。

### 黒帯

任意で表示します。文字色は黄→金の固定グラデーションで、縁取りはありません。ロゴが左なら帯は右に移ります。

色は参考PNGの画素から近似した固定値です。元の編集データ・使用書体・グラデーション設定自体を復元したものではなく、参考画像とのピクセル単位の完全一致を保証するものではありません。

## 背景・画像

背景は画面を隙間なく埋めるようにトリミングします。「トリミング・見やすさ」で拡大率、ぼかし、文字の背後の暗さ、左右反転を調整できます。「背景をドラッグで調整」を有効にすると背景だけを動かせます。ホイールは背景調整中のみ拡大率を変えます。

「画像」タブには専用キャラクター1枠と追加画像5枠があります。**背景・ロゴ・専用キャラクターは5枚に数えません。** キャラクターは最初に背景と文字の間へ大きく配置します。透明な余白は自動で削除しますが、非透過画像からの人物・キャラクターの自動切り抜きは行いません。

PNG / JPG / WebP / AVIFを受け付けます。1枚20MB、4,000万ピクセル以下です。長辺4096pxを超える画像は読み込み時に縮小します。GIF・SVG・動画・PSDは非対応です。解像度不足や画面外に出たレイヤーは「仕上がりチェック」に表示します。

### レイヤー操作

画像・自由文字をドラッグすると移動します。四隅のハンドルは縦横比を固定した拡縮、丸いハンドルは回転です。数字でも座標・幅・角度・不透明度を指定できます。位置はオブジェクトの中心座標です。画像の「大きさ」は幅、自由文字は自動算出サイズに対する倍率です。

「重ね順」は上の行ほど手前。行のドラッグ、または矢印ボタンで並べ替えます。追加画像を文字やロゴより手前に置くこともできます。背景だけは最背面固定です。表示切替、左右反転、位置ロック、複製、削除に対応しています。

「空きスペースへ」は文字・ロゴ・ほかの画像との矩形の重なりが少ない候補へ移します。人物の顔や被写体の重要性を認識するAIではありません。最終位置はプレビューで確認してください。「自動で整える」はロックしていないレイヤーを再配置します。

## SET 18 の素材サイト

指定先： https://gamers-hack.com/tftimg/set-18

「SET 18 素材」は2026年9月5日に指定サイトから抽出した4,844件の画像URL・名前を同梱しています。一覧は外部サイトへ通信せず読み込むため、GitHub Pages、ローカルHTML、単体HTMLで利用できます。名前検索、保存したHTML/素材一覧JSONの読込も可能です。一覧は自動更新されません。

素材はカテゴリ別のタブで表示し、初期選択は「チャンピオン」です。タブにはカテゴリごとの件数が表示され、検索は選択中のカテゴリ内が対象です。200件を超える場合は「さらに200件表示」で続きが見られます。タブは左右矢印・Home・Endキーでも切り替えられます。持ち込んだ一覧のうち既知の素材URLに分類できない画像は「その他」に表示します。

画像本体は外部配信元から取得するため、表示・追加にはインターネット接続が必要です。同梱一覧の配信元は外部読込を許可していますが、ネットワーク制限や画像の削除によって取得できない場合は「手元の画像」を使用してください。外部プロキシは使っていません。

一覧を持ち込む場合は、素材選択画面の「素材一覧を保存」リンクをブックマークバーへドラッグしてください。素材サイトを開いてからそのブックマークを押すと、表示中ページの画像URL一覧をJSONで保存します。このJSONをツールへ読み込みます。必要な画像をページ上で読み込み・表示してから実行してください。画像本体のCORS制限は解除されません。

「画像URL」は画像そのもののURLを指定します。読込に失敗した際は、保存した画像を使ってください。読み込んだ画像の利用条件は、配布元の案内を確認してください。

## 書き出しとフォント

PNG / JPEG、1280×720 / 1920×1080 / 2560×1440に対応しています。PNGを標準にしています。JPEGは品質0.96です。1280幅は2倍解像度で一度描いてから縮小します。プレビューと書き出しは同一の描画処理を使い、選択枠・ガイド・UIは出力しません。

指定フォントが読み込めていない場合は、無断で代替フォントを書き出さないようダウンロードを止めます。接続を確認して「再試行」を押してください。「操作のヒント」から手元の指定フォント（900 / Black）を読み込む方法もあります。ローカルフォントの書体・ウェイトはファイル提供元で確認してください。ツールはファイルの書体の正当性までは判定しません。

急ぎの場合は「代替フォントでの書き出しを許可」にチェックすると出力できます。この場合は参考画像とは字形が変わります。ローカルフォントはタブを閉じると読み込み直しが必要です。

画像コピーはブラウザのクリップボード権限と実行環境に依存します。利用できない環境ではダウンロードしてください。

## 保存

編集内容と画像はIndexedDBへ自動保存します。最近使った24素材と現在のプロジェクトを保存し、不要な古い画像を保存領域から整理します。自動保存の成功・失敗は左下に表示します。ブラウザが保存領域を許可しない場合、プライベートモードの制限、容量不足などで使えないことがあります。

「プロジェクトを保存」では画像を内包した `.tft.json` をダウンロードします。「プロジェクトを開く」で配置・色・強調・画像を復元できます。フォントそのものは保存しません。不正なファイルは、編集中の内容を置き換える前に検証します。プロジェクト読込の上限は100MBです。

自動保存だけに頼らず、完成したサムネイルの編集データはファイル保存してください。ファイルの置き場所・ブラウザ・オリジンを変えると、同じ自動保存が見えない場合があります。復元には保存した `.tft.json` を使用してください。

## ショートカット

| 操作 | キー |
|---|---|
| 元に戻す / やり直す | Ctrl+Z / Ctrl+Shift+Z（Ctrl+Yも対応） |
| 1px移動 / 10px移動 | 矢印 / Shift+矢印 |
| 移動方向の固定 | Shift+ドラッグ |
| 吸着を一時解除 | Alt+ドラッグ |
| 15度刻みの回転 | Shift+回転 |
| 複製 / 削除 | Ctrl+D / Delete |
| プロジェクト保存 | Ctrl+S |
| 書き出し画面 | Ctrl+Shift+E |
| 選択解除・背景調整終了 | Esc |

MacではCtrlの代わりにCommandを使います。日本語変換中はショートカットで編集状態を変更しません。Undoは最大60段階です。

## 技術・検証

HTML / CSS / Vanilla JavaScript / Canvas 2D。React・Vue・バックエンド・ランタイムのビルドツールは使っていません。

UIにはG-Force UIのTailwindビルド済みCSSとJavaScriptを使用しています。同梱版は1.0.0を固定し、必要なエディタ固有のレイアウトを追加しています。JavaScriptはfile://用のclassic-scriptラッパーと、保存領域制限時の例外防止を加えています。G-Force UIのMITライセンスは `vendor/GFORCE-LICENSE.txt` にあります。

実行した48項目のテストと確認範囲は `TESTING.md`、設計は `ARCHITECTURE.md` を参照してください。単体版の再生成は `python build_standalone.py` です。
